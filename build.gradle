allprojects {
    repositories {
        mavenCentral()
    }
    buildscript {
        repositories {
            mavenCentral()
        }
    }
}

apply plugin: 'groovy'

task wrapper(type: Wrapper) {
    gradleVersion = '2.0'
}