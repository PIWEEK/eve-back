apply plugin: 'groovy'

allprojects {
    repositories {
        jcenter()
    }
    buildscript {
        repositories {
            jcenter()
        }
    }
}

sourceCompatibility = 1.7
targetCompatibility = 1.7

task wrapper(type: Wrapper) {
    gradleVersion = '2.0'
}
